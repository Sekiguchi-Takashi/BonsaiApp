# v1.3 — frontmatter 対応（RAG生成プロジェクト 第一弾の受け皿）

配布キットが生成する資料を、アプリ側で正しく扱えるようにした。

## 追加: `rag/Frontmatter.kt`
ファイル先頭の `---` で囲まれたメタデータを解析する。

```
---
type: app_doc
app_name: KakeiApp
---
（本文）
```

YAML完全実装はしない。`key: value` の1行1項目だけ。
**frontmatter が無い従来ファイルもそのまま通る**（後方互換）。

## RagDb の変更（DBバージョン 1→2）
- `docs` テーブルに `doc_type` と `title` 列を追加
- インデックス時に frontmatter を分離し、`type` を記録
- **frontmatter の値も検索対象に含める**。
  「KakeiApp」「白狐のコン」のような固有名で引けるようになる
- `search(typeFilter=...)` で種別を絞れる。
  空なら全件、"app_doc" / "character" で限定。
  **将来のキャラ絞り込み・GMモードの土台**（今はUIから未使用）
- `typeCounts()` で種別ごとの文書数を取得

## RAG設定画面
統計に種別内訳を表示。
「文書 12 / チャンク 18 / 語彙 3200 / app_doc:10 character:2」のように出る。
どの種別が何件入っているか一目で分かる。

## DBマイグレーション注意
列追加のため DB バージョンを上げた。
現状の onUpgrade は**テーブルを作り直す**実装なので、
更新後の初回に一度「インデックス更新」を実行すること
（既存インデックスは消えるが、フォルダから作り直せる）。

## この版でできること
配布キット（rag_kit.zip）で生成した資料を RAG フォルダに入れて
インデックス更新すれば、
- type や固有名で検索が効く
- 種別ごとの件数が見える

## まだ無いもの（次段以降）
- UIからの type 絞り込み（検索は既に対応済み、ボタンが無いだけ）
- アプリ内での資料作成・編集（書き込み機能）
- アプリ台帳（どのアプリ/キャラが未作成か）
- キャラ応答モード（設定検索は可能、なりきりは将来）
