# v1.6.2 — doc_part を検索対象に戻した

## 背景
v1.3 では frontmatter の全項目を本文先頭に連結していたため、
すべてのメタ値が検索できた。

v1.6 で「全チャンクの先頭に主題名を付与する」方式へ変えた際、
タグに含めるのを `app_name` と `app_label` だけに絞ってしまい、
**`doc_part` が検索対象から漏れていた**。

## 影響
1アプリを機能編・実務編に分割する運用（抽出プロンプトv2）では、
「MemoApp の実務」のような絞り込みができなかった。
本文中に「実務」の語が出ないため、どちらのファイルも同じに見える。

## 修正
チャンク先頭のタグに `doc_part` を追加。
`MemoApp メモ帳 機能` / `MemoApp メモ帳 実務` が各チャンクの先頭に入る。

## 更新後にやること
インデックスの作り直しが必要（RAG設定 → 全消去 → インデックス更新）。
v1.6 から更新する場合も同様。
