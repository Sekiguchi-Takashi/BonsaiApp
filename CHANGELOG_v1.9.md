# v1.9 — モデルを専用フォルダから直接読む

## 変更の理由
v1.8 までは選んだ .gguf を**アプリ内部へコピー**していた。

- 570MB のファイルが端末に2つ存在する
- アンインストールするとコピーが消え、入れ直しが必要
- 取り込み時にモデルサイズ+64MB の空きが要る

## 新方式（コピーしない）
モデル専用フォルダを SAF で1つ登録し、**その場所のファイルを直接読む**。

`ContentResolver.openFileDescriptor()` で得た記述子から
`/proc/self/fd/<番号>` を組み立て、llama.cpp にパスとして渡す。
llama.cpp 側は通常のファイルとして開くので **mmap もそのまま効く**。

記述子はモデルを解放するまで保持する（先に閉じるとパスが無効になるため）。
解放順は「llama.free() → ModelStore.release()」。

## 動作

| 状況 | 動作 |
|---|---|
| 起動時、フォルダに .gguf が1つ | 自動で読み込む |
| 起動時、複数ある | 前回使ったファイルを読む |
| 「モデル」を押し、複数ある | 選択ダイアログ（「フォルダを変更」付き） |
| **フォルダに .gguf が無い** | **エラーのポップアップ** |
| **フォルダ未設定** | **エラーのポップアップ → フォルダ選択へ** |
| 読み込み失敗 | 原因の候補を出すポップアップ |

ポップアップは `setCancelable(false)` なので、
戻るキーでは閉じずに必ず選択させる。

## 追加・変更ファイル
- `ModelStore.kt`（新規）— フォルダ登録、.gguf 一覧、記述子の管理
- `MainActivity.kt` — コピー処理を撤去し、フォルダ方式に置き換え
- `ServerService.kt` — サービス側の自前読込も新方式に対応
- `ManualActivity.kt` — 手順を書き換え、FAQ に「モデルが見つかりません」を追加

## 更新後にやること
**フォルダの登録が必要です。**
更新後の初回起動でポップアップが出るので、
.gguf を置いたフォルダを選んでください。

例: `/sdcard/Download` をそのまま指定してもよいですが、
モデル専用のフォルダ（例: 内部ストレージ/Models）を作って
そこに置くほうが管理しやすくなります。

## 想定されるリスク
`/proc/self/fd/` 経由の読み込みは、端末やストレージの種類によっては
失敗する可能性があります（特に SD カードやクラウド系プロバイダ）。
内部ストレージであれば問題なく動くはずですが、
読み込みに失敗する場合は原因を出すポップアップが表示されます。
その場合は報告してください。コピー方式へ戻す判断をします。
