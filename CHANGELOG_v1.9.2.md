# v1.9.2 — 直接読みが失敗する端末への対応

## 症状
v1.9 で「モデルを読み込めませんでした」。
空きRAM 901MB、ファイルは Bonsai-4B-Q1_0.gguf（約570MB）。
容量・RAM ともに足りているのに失敗した。

## 原因
`/proc/self/fd/N` は**実ファイルへのシンボリックリンク**である。
llama.cpp がこのパスを開き直すと、リンク先の
`/storage/emulated/0/Models/...` を直接開こうとする。
スコープドストレージではアプリがこのパスへ直接アクセスできないため拒否される。

SAF の記述子は「その fd 経由でのみ」有効で、パスとして再オープンする
使い方は保証されていなかった。v1.9 の設計上の誤りである。

## 修正: 2段構え
1. **まず直接読みを試す**（コピー不要・容量が二重にならない）
2. 失敗したら**アプリ内部へキャッシュしてから読む**

キャッシュはファイル名とサイズで一致判定するので、**コピーは初回だけ**。
2回目以降の起動は再利用され、待たされない。
コピー中は進捗（MB）を表示する。

読み込み後、ステータスにどちらで読んだかを表示する。
　例: `読込完了 1200ms / Bonsai-4B-Q1_0.gguf（キャッシュ）`

## 維持される利点
- モデルフォルダの登録は1回だけ
- 起動時の自動読み込み
- **元ファイルはフォルダに残るので、アプリを消しても再ダウンロード不要**
  （v1.8 まではアンインストールでモデルごと消えていた）

## 代償
キャッシュ経路になった場合、モデルのサイズぶん容量を余分に使う。
`ModelStore.clearCache()` で削除できる（UIからの導線は未実装）。

## その他
- ServerService 側の読み込みも同じ2段構えにした
- 失敗時のポップアップに**原因の文言**を表示するようにした
- マニュアルにキャッシュの説明を追記

## 検証
v1.9.1 と全ファイルの関数一覧を照合し、欠落が無いことを確認済み。
