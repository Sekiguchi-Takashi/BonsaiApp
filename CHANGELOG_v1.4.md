# v1.4 — バグ修正と検索の高速化

コードレビューで見つけた実バグ4件＋設計上の問題2件の修正。機能追加なし。

## バグ修正

### 1. typeFilter で検索件数が欠ける（RagDb）
v1.3 で入れた種別絞り込みが、**上位 limit 件を取った後**にフィルタしていた。
app_doc だけで絞ると、上位3件に character が混ざっていた分だけ
返却件数が減る。フィルタ→limit の順に修正。
（現状 UI から typeFilter は未使用のため実害は未発生。将来の地雷を撤去）

### 2. エンジン使用中の2本目のリクエストが黙って死ぬ（LlamaServer）
推論は直列化されているため、実行中に来た2本目の HTTP リクエストは
ロック待ちのまま soTimeout の30秒で接続を切られていた。
呼び出し側からは原因不明の失敗に見える。**503 (engine busy) を即返す**ように修正。
KakeiApp 等から叩く時はリトライで対処できる。

### 3. メールの二重取り込み判定に競合の穴（MailQueue）
check-then-insert 方式だったため、UNIQUE 衝突時に insert が失敗しても
true を返していた。`CONFLICT_IGNORE` で原子的に判定するよう修正。

### 4. fetchOnce のソケット競合（MailWatcher）
監視スレッドと同じ ImapClient インスタンスを共有しており、
稼働中に呼ぶと競合する設計バグ。UIからも未使用だったため撤去。

### 5. HTTP レスポンスに Connection: close が無い（LlamaServer）
サーバーは1リクエストごとにソケットを閉じるのに、それをクライアントに
伝えていなかった。接続再利用を試みたクライアントが失敗する。
SSE 以外のレスポンスに `Connection: close` を付与。

### 6. モデル取込前に空き容量を確認しない（MainActivity）
コピー途中で容量切れになり、進捗表示のまま失敗していた。
SAF からサイズを取得し、不足時は必要量を表示して中断するよう修正。

## 性能改善

### BM25 のクエリ統合（RagDb）
語ごとに df 取得＋posting 取得の**2クエリを発行**していた（計 2N 回）。
日本語 bigram はクエリが30語を超えることもあり、SQLite の往復回数が
検索時間に直結していた。**1回の IN 句**に統合し、df はメモリ上で数える。
資料が増えるほど効く（数十msレベルの改善だが、検索は毎回走る）。

## 直していない性能課題（正直に）

1. **RAG時の初回応答が遅い（10〜20秒）** — 資料900文字のプリフィルが支配的。
   根本対策は KV キャッシュのプレフィックス再利用（システムプロンプト分の
   状態保存）だが、llama.cpp の state API を使う中規模改修になる。
   効果は初回応答の2〜3秒短縮程度で、費用対効果を考えて見送り
2. **スレッド数が当てずっぽう** — `availableProcessors - 2` は big.LITTLE 構成で
   最適とは限らない。実測ベンチ（2/4/6スレッドで各1回生成して比較）を
   入れる価値はあるが、現状 3.9 tok/s で足りているなら急がない
3. **strip() が生成中に毎回全文へ正規表現をかける** — 出力4KB程度なら
   実測誤差レベルのため放置
