# v1.8 — 使い方マニュアルとタグ必須モード

## 1. アプリ内マニュアル（新規）

上段に「使い方」ボタンを追加。`ManualActivity` に全文を埋め込んでいる
（外部ファイル無し・ゼロ依存方針を維持）。

内容: 最初の3ステップ / 質問の仕方 / タグの説明 / 資料エディタ /
サーバー / メール連携 / よくある質問6件 / 注意事項

## 2. タグ選択モード（RAG参照 ON 時）

### 背景
3アプリで検証したところ、アプリ名を書かない質問
（例:「録音の保存形式は？」）で MemoApp と MusicRoomApp の
資料が混ざった。40本規模ではこの衝突が常態化する。

### 変更
RAG参照のボタンを**左半分**に縮め、**右半分にタグ選択**を置いた。

- タグは frontmatter の `app_name`（キャラなら `character_name`）
- 資料を追加してインデックスを更新すると自動で選択肢が増える
- **RAG参照 ON でタグ未選択のときは生成できない。**
  「生成」を押すとタグ選択ダイアログが開く
- RAG参照 OFF のときはタグ欄が無効化される（「（タグ不要）」表示）

### 検証結果
「録音の保存形式は？」というアプリ名なしの曖昧な質問で、
タグ=MemoApp なら MemoApp の資料だけ（.m4a を含む）、
タグ=MusicRoomApp なら MusicRoomApp の資料だけが返ることを確認。

### 実装
- `RagDb.listTitles()` — インデックス済みタグの一覧
- `RagDb.searchTwoStage(titleFilter=...)` — タグで検索対象を限定
- `Pipeline.retrieve(query, tag)` / `Pipeline.answer(tag=...)`

## 3. ナビゲーションを 3列×2行 に

「使い方」が増えたため、横1列（v1.7）から2行に変更。

- 1行目: モデル / サーバー / 使い方
- 2行目: RAG設定 / エディタ / メール

入力欄の位置は v1.7 とほぼ同じで、キーボードで隠れない。

## 適用範囲について

タグ必須はトップ画面の生成のみ。
RAG設定画面の「検索テスト」は診断用途のため、
従来どおり全資料を対象に検索する（タグ指定なしで挙動を確認できる）。

## 更新後にやること
インデックスの作り直しは不要。
ただし**タグが空の資料は選択肢に出ない**ため、frontmatter に
`app_name` が無い資料は追記してインデックスを更新すること。
