# v1.5 — 資料エディタ（フェーズ4-a）

アプリ内で資料を書き、RAGフォルダへ保存し、その場で再インデックスまで行う。

## 追加: `EditorActivity.kt`

- **テンプレート内蔵**: アプリ資料 / キャラ設定 / フリーの3種。
  配布キットと同一の frontmatter 形式。updated には当日日付が自動で入る
- **開く**: フォルダ内の .md/.txt 一覧から選んで読み込み、編集して上書き保存
- **保存して反映**: 保存後に差分インデックスを自動実行。
  「保存・反映完了（更新1 変更なし11）」のように結果を表示
- ファイル名検証（`/` `\` `..` を拒否、拡張子なしは .md を付与）
- 等幅フォント、キーボード表示時のリサイズ対応

## FolderSync の変更

- `listTextFiles()` / `readFile()` / `writeFile()` を追加
- `writeFile` は**同名があれば上書き（サブフォルダ内も検索）、無ければ直下に新規作成**
- フォルダの永続権限を**読み取り専用 → 読み書き**に変更

## ⚠️ 移行手順（必須が2つ）

### 1. フォルダの選び直し
v1.4以前に選んだフォルダは読み取り権限しか持っていない。
**RAG設定 → フォルダを選択** で同じフォルダをもう一度選ぶこと。
やらないと保存時に「書き込み権限がありません」になる。

### 2. rclone の運用変更（重要・データ消失防止）
従来の同期コマンドは使わないこと:

```
rclone sync od:RAG ~/storage/shared/RAG   ← 今後は禁止
```

`sync` は同期先を同期元へ**完全一致**させるため、
**アプリで作ったローカルのファイルが OneDrive に無い＝削除対象**になる。
エディタで書いた資料が次の同期で消える。

今後は copy を双方向で使う:

```
rclone copy ~/storage/shared/RAG od:RAG
rclone copy od:RAG ~/storage/shared/RAG
```

copy は削除を伝播しないので、どちらで作ったファイルも残る。
（トレードオフ: ファイル削除は両側で手動になる。
 削除同期が必要になったら rclone bisync の導入を検討する）

## 使い方

1. トップ →「資料エディタ」
2. テンプレートボタンで書式を挿入（3種）
3. ［ ］の箇所を埋める
4. 「保存して反映」→ その場で検索対象になる
5. トップに戻って RAG参照 ON で検索テスト

OneDrive へ上げたい時に Termux で上記の copy を実行する。
