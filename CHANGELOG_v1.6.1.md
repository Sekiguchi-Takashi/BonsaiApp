# v1.6.1 — ビルド失敗の修正

## 症状
v1.6 が CI でコンパイルエラー。

```
FolderSync.kt:100 Unresolved reference: allDocIds
```

## 原因
v1.6 で 2段階検索を入れる際、`search()` からファイル末尾側へ向けて
まとめて置換した。その範囲の終端指定が甘く、**`search()` の直後に
定義されていた `allDocIds()` まで巻き込んで削除**していた。

`allDocIds()` は FolderSync が「フォルダから消えたファイルを
インデックスからも削除する」ために使う関数で、RagDb 内からは
呼ばれていないため、RagDb 単体を見ても欠落に気づけなかった。

## 修正
`allDocIds()` を復元。あわせて全 .kt ファイルを走査し、
他に同種の参照切れが無いことを確認済み（他は0件）。

## 機能面の変更
なし。v1.6 の内容（チャンクへのアプリ名付与、2段階検索、
チャンクサイズ拡大）はそのまま。

## 更新後にやること
v1.6 と同じく、**インデックスの作り直しが必要**。
RAG設定 →「インデックス全消去」→「インデックス更新」。
