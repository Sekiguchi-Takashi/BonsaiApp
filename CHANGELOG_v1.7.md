# v1.7 — キーボードで入力欄が隠れる問題の修正

## 症状
トップ画面のプロンプト入力欄をタップすると、ソフトキーボードに隠れて
入力中の文字が見えない。

## 原因
ナビゲーション用のボタン5つ（モデル／サーバー／RAG設定／資料エディタ／
メール連携）を**全幅で縦積み**していたため、入力欄が画面のかなり下に
位置していた。キーボードが出ると画面の下半分が塞がれ、入力欄が
その外に押し出される。

## 修正
- 5つのボタンを**横1列**にまとめた（モデル／サーバー／RAG／エディタ／メール）。
  約4行ぶんの高さが空き、入力欄が画面上部に上がる
- マニフェストの MainActivity に `windowSoftInputMode="adjustResize"` を追加。
  キーボード表示時に出力欄が縮み、入力欄が押し出されなくなる
- ボタンのラベルを短縮（横並びに収めるため）

## 併せて修正
`allDocIds()` が欠落していたのを復元（v1.6.1 と同じ修正）。
v1.6.2 のパッケージにこの修正が入っていなかったため、
そのままではビルドが通らない状態だった。
全 .kt を走査して他に参照切れが無いことを確認済み。

## 変更なし
2段階検索、チャンクへのアプリ名付与、doc_part の検索対象化は
v1.6.2 のまま。インデックスの作り直しは不要（v1.6系から更新する場合）。
