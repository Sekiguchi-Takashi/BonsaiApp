# v1.6 — 40アプリ規模への対応

資料が1〜2本の時は問題にならず、**数十本で顕在化する**3つの問題を潰した。

## 1. チャンクにアプリ名が入らない（修正）

v1.3 の frontmatter 対応は、メタデータを本文の先頭に連結していただけ。
そのため**2つ目以降のチャンクにアプリ名が含まれない**。
「IroGameのかくれんぼは？」で絞り込めず、他アプリと混ざる。

→ 全チャンクの先頭に `app_name` と `app_label`（キャラなら
   `character_name` と `story`）を自動付与するよう変更。

## 2. IDF の希釈（本命の修正・2段階検索の導入）

40本入れると「操作手順」「結果」「設定画面」といった語が全文書に出現する。
BM25 は「多くの文書に出る語＝重要でない」と判断するため、
**共通語の重みがほぼ0になり、固有名だけが頼りになる**。
1本では気づかないが、本数に比例して悪化する構造的な問題。

→ **2段階検索**を導入。
   1. まず文書単位でスコアを集計し、上位3文書に絞る
   2. その文書内のチャンクだけで本検索を行う

   検索対象が1〜3アプリに限定されるため、共通語の希釈が起きない。
   `searchTwoStage()` として実装し、パイプラインと検索テストの両方で使用。

## 3. チャンクが小さすぎる（調整）

IroGame の「たんけん」の説明は約800文字あり、500文字チャンクだと分断される。
40本から上位3件では1機能の説明すら揃わない。

| 項目 | v1.5 | v1.6 |
|---|---|---|
| CHUNK_CHARS | 500 | **700** |
| CHUNK_OVERLAP | 100 | **120** |
| TOP_K | 3 | **4** |
| MAX_CONTEXT_CHARS | 900 | **1000** |

n_ctx=2048 の予算内に収まる範囲での拡大。

## その他

- `Hit` に `docId` と `title` を追加
- 出典表示をファイル名から**アプリ名**（frontmatter の app_name）に変更。
  「参照: いろあてゲーム」のように出る
- deploy.sh のタグ対象SHAを `git rev-parse HEAD` に変更
  （push直後に API を叩くと反映待ちで一つ前のコミットにタグが付くため）

## 更新後にやること

**インデックスの作り直しが必要。**
チャンク分割とチャンク内容が変わるため、既存インデックスは古い形式のまま。
RAG設定 →「インデックス全消去」→「インデックス更新」を実行すること。

## 40本入れた後の確認方法

1. 「IroGameのかくれんぼは？」→ IroGame のチャンクだけが上位に来るか
2. 「操作手順を教えて」のような共通語だけの質問 → 極端に無関係な結果に
   ならないか（この質問自体は本来絞り込めないので、結果が散るのは正常）
3. 検索テストの score 表示で、1位と2位の差が付いているか
   （僅差が並ぶ場合は絞り込めていない兆候）
